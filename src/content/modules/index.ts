/* Ship modules. A module is data plus a hook handler — nothing else.
 * Power draw is the constraint that makes finding a good one a decision.
 *
 * Empty until M3.
 */

import type { ModuleDef } from '../../engine/types.ts';

export const MODULES: readonly ModuleDef[] = [];
